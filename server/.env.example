# Copy this file to .env and fill in real values.
# .env is gitignored.

# Required for POST /validate-config (the LLM call).
# Without it, /validate-config returns 502.
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional — defaults to 3000 / 0.0.0.0.
PORT=3000
HOST=0.0.0.0
